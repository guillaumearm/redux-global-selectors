import * as index from '../src';

/* eslint-disable no-console */
describe('react-redux-global-selectors', () => {
    describe('exports', () => {
        it('have a src/index.js file', () => {
            expect(index).toBeDefined();
        });
    });
});
